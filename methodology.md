# 생활권 분석 방법론 세부사항

2026-09-14 · 현재 웹 지도에서 사용하는 구획 기준

## 1. 자료와 분석 단위

2024·2025년의 1km 격자 간 기종점(OD) 통행 자료를 사용한다. 전체·출근·주중·주말의 네 가지 목적별로 구획하며, 결과를 조회할 때 연도와 통행 목적을 함께 명시한다. 통행량 표시는 일평균 기준이다. 그래프 구성에는 출근 자료의 VOL>2, 전체·주중·주말 자료의 일평균 5통행 이상 선별 규칙을 적용한다. 자기 격자 안 통행도 포함한다. 격자 제외·예외 처리는 결과 파일에 저장된 공통 규칙을 따른다.

## 2. 레이든의 기본 원리

레이든은 네트워크에서 밀접하게 연결된 지점들을 같은 집단으로 묶는 알고리즘이다. 이 연구에서는 격자를 지점으로, 격자 사이의 통행량을 연결의 강도로 삼아 생활권을 찾는다.

모듈러리티는 같은 집단 안의 연결이 기대 수준보다 얼마나 강한지를 평가한다. 각 격자의 전체 통행 규모를 유지한 채 연결이 무작위로 이루어진다고 가정한 기준과 실제 내부 통행량을 비교한다. 기대보다 내부 통행이 많은 구획일수록 높은 점수를 받는다. 레이든은 점수를 높이는 방향으로 소속 이동, 집단 내부 정련, 집단 단위의 재탐색을 반복한다. [알고리즘과 모듈러리티의 정의](https://leidenalg.readthedocs.io/en/stable/reference.html#optimiser)

해상도 γ는 기대 통행량에 부여하는 가중치를 조절한다. γ=1이면 표준 모듈러리티 기준이며, γ가 클수록 대체로 작고 세밀한 집단을 찾는다. 기본 레이든 지도에서는 전국에 같은 γ를 적용한다. [해상도를 포함한 목적함수](https://leidenalg.readthedocs.io/en/stable/reference.html#rbconfigurationvertexpartition)

### 두 방법의 적용 기준

| 항목 | 레이든 | 자족도 기반 레이든 |
| --- | --- | --- |
| 해상도 | 전국 공통 γ | 5극3특별 γ |
| 조절 기준 | γ별 구획의 세밀함 | 목표 자족도 50~90%, 2.5%p 간격 |
| 실행 | 각 γ에서 초기 조건을 바꿔 반복 실행 후 합의 | 목표마다 독립 실행, 10시드 기반 해상도 보정 |
| 경계 처리 | 공간·소규모 권역 후처리 | 경계 쌍 γ는 두 지역 중 최댓값, 기존 후처리 유지 |
| 개별 자족도 하한 | 목표−15%p 하한 보정 없음 | 목표−15%p 하한에 미달하면 인접 권역과 병합 |

‘자족도 기반 레이든’은 이 연구에서 사용하는 모형의 표시 이름이다. 5극3특은 지역별 해상도 보정의 단위이며 생활권의 고정 경계가 아니다. 생활권은 5극3특·시도 경계를 넘을 수 있다. ‘레이든’ 화면 역시 원형 라벨에 공간·소규모 권역 후처리를 적용한 결과이므로 순수한 무보정 알고리즘 결과와 구별한다.

## 3. 자족도 기반 레이든

레이든의 통행 연결 분석에 자족도 기준을 더한다. 전국을 함께 구획하면서 5극3특별 해상도를 조정해 목표 자족도에 접근하고, 개별 생활권에는 별도의 하한을 적용한다.

### 계산 절차

1. 선택한 연도·목적의 전국 격자 OD 그래프를 구성한다.
2. 5극3특별 해상도 γ를 조정한다. 지역 경계를 넘는 쌍에는 `max(γ_a, γ_b)`를 사용하여 전국 단일 목적함수에서 구획한다.
3. 후처리 결과의 목적별 출발 기준 통행 자족도를 평가한다. 목표와 가까운 결과를 고르는 nearest 규칙으로 해상도를 보정한다.
4. 목표 T에 대해 개별 생활권 출발 기준 자족도 하한 `T−15%p`를 적용한다. 하한 미달 권역은 인접 권역과 통째로 병합한다. 병합 상대는 자족도 부족량 감소, 인구 부족량 감소, 양방향 통행량(flow)의 순서로 평가한다. 이 병합 단계에서는 기존 생활권을 분할하거나 γ를 다시 탐색하지 않는다.
5. 최종 격자 소속·권역 대표·목표별 통계와 파생 자료를 저장한다. 2개 연도 × 4개 목적 × 17개 목표에 동일한 설정을 적용한다.

### 자족도 목표와 생활권별 하한

생활권 r의 내부 통행을 I_r, 전체 출발 통행을 O_r, 전체 도착 통행을 D_r라고 하면:

- 출발 기준 자족도: `SC_r = I_r / O_r × 100`
- 내부 공급률: `I_r / D_r × 100` (해석 지표)
- 전국 통행 자족도: `Σ I_r / Σ O_r × 100`

전국 통행 자족도는 생활권별 자족도를 출발 통행량으로 가중한 평균이다. 통행의 출발지와 도착지가 같은 생활권에 속하는 비율을 뜻한다.

목표 70%일 때 개별 생활권 자족도 하한은 55%이다. 목표 자체가 모든 생활권의 자족도를 뜻하지 않는다. 병합 후 실제 전국 통행 자족도는 목표를 초과할 수 있다. 예를 들어 2025년 출근·목표 70%의 최종 결과는 108개 생활권, 전국 통행 자족도 약 73.46%이다.

15%p는 실험을 통해 채택한 절충 설정이다. 70% 역시 기본 조회값이며 모든 분석에 유일한 최적값이라는 뜻은 아니다. 인구 2만 관련 후처리가 있지만 고립된 예외 격자까지 모두 인구 2만을 충족한다고 해석해서는 안 된다.

## 4. 권역별·시도별 지도와 좌우 비교

지역별 지도는 선택한 알고리즘의 전국 결과를 5극3특 또는 시도 경계로 잘라 보여준다. 해당 지역 내부 OD만으로 다시 구획하지 않는다. 지역 내 권역 수는 경계와 교차하는 생활권의 수이며, 옅게 표시한 주변 지역은 지역 집계에서 제외한다. 자족도 기반 레이든의 개별 자족도 하한은 행정경계로 자르기 전 생활권 전체에 적용된다.

좌우 지도에서 연도·목적·알고리즘·규모·대상 지역을 각각 설정할 수 있다. 지도 이동과 확대, 색·투명도 등 표시 설정은 공유한다. 레이든의 규모는 실제 γ별 결과로, 자족도 기반 레이든의 규모는 목표 자족도로 고른다. 같은 숫자가 두 모형에서 동일한 의미를 갖는 것은 아니다.

## 5. 해석 시 유의사항

목표별 구획은 독립적으로 계산된다. 목표를 높이면 생활권이 합쳐지기만 하는 계층 구조나 목표 사이 소속의 단조성은 보장하지 않는다.

연도별 구획의 차이에는 통행 자료의 변화와 독립 최적화에 따른 변동이 함께 반영된다. 지도에서 나타나는 소속 변경을 관측된 사회경제적 변화로 곧바로 해석하지 않는다.

## 6. 저장소 내 재현 근거

- 채택 모델과 검증 기록: `artifacts/final_model_m15/README.md`
- 해상도 보정: `scripts/bnrt/hetero_gamma.py`
- 하한 병합: `scripts/bnrt/constrained_leiden.py`, `scripts/bnrt/build_final_model.py`
- 전국 공통 레이든: `scripts/bnrt/leiden_indep.py`
- 현재 자족도 기반 레이든 결과: `public/data/{연도}/living/bnrt_floor_repair_{목적}_m15_flow_indep.json`
- 지역 확대: `src/hooks/useScopeLayer.ts`
